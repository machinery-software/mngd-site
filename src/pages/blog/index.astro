---
import Layout from '../../layouts/Layout.astro';

const posts: {
  slug: string;
  title: string;
  date: string;
  category: string;
  excerpt: string;
}[] = [
  // {
  //   slug: 'introducing-mngd',
  //   title: 'Introducing mngd',
  //   date: '2026-04-07',
  //   category: 'Announcement',
  //   excerpt: '...',
  // },
];
---

<Layout
  title="Blog — mngd"
  description="Product announcements, changelog, and MDM deep-dives from Machinery Software."
>

  <section class="section pt-28">
    <div class="container max-w-3xl">

      <div class="mb-12">
        <div class="label mb-4">Blog</div>
        <h1 class="text-4xl font-semibold text-white tracking-tight">
          Updates from mngd.
        </h1>
      </div>

      {posts.length === 0 ? (
        <div class="card p-12 text-center">
          <div class="text-xs font-mono text-white/20 mb-6 tracking-widest">COMING SOON</div>
          <h2 class="text-white font-semibold text-lg mb-3">First post incoming.</h2>
          <p class="text-sm text-white/40 leading-relaxed">
            Product updates, MDM deep-dives, and engineering notes from Machinery Software.
            Follow along at{' '}
            <a href="https://x.com/mngdapp" class="text-white/60 hover:text-white transition-colors underline underline-offset-2">@mngdapp</a>.
          </p>
        </div>
      ) : (
        <div class="space-y-4">
          {posts.map(post => (
            <a href={`/blog/${post.slug}`} class="card p-6 flex items-start gap-6 hover:border-white/10 transition-colors block">
              <div class="text-xs text-white/30 font-mono pt-0.5 whitespace-nowrap">
                {new Date(post.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
              </div>
              <div>
                <div class="text-[11px] font-semibold uppercase tracking-widest mb-1" style="color:#6B82F0;">{post.category}</div>
                <h2 class="text-white font-semibold text-[15px] mb-1">{post.title}</h2>
                <p class="text-sm text-white/45 leading-relaxed">{post.excerpt}</p>
              </div>
            </a>
          ))}
        </div>
      )}

    </div>
  </section>

</Layout>
